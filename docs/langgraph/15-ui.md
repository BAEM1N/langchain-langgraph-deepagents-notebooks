# Agent Chat UI Documentation

## Overview

Agent Chat UI is a Next.js application offering a conversational interface for LangChain agents. It features "real-time chat, tool visualization, and advanced features like time-travel debugging and state forking" and integrates seamlessly with agents built using `create_agent`.

## Key Features

- Interactive chat interface for LangChain agents
- "Out-of-the-box support for rendering tool calls and tool result messages"
- Time-travel debugging capabilities
- State forking functionality
- Support for generative UI
- Open source and customizable
- Works with agents created via the `create_agent` factory

## Getting Started

### Hosted Version
1. Visit the hosted application at https://agentchat.vercel.app
2. Enter your agent's deployment URL or local server address
3. Begin chatting -- the interface automatically detects and renders tool calls and interrupts

### Local Development Setup

Two installation options are available:

**Using npx:**
```bash
npx create-agent-chat-app --project-name my-chat-ui
cd my-chat-ui
pnpm install
pnpm dev
```

**Via Git clone:**
```bash
git clone https://github.com/langchain-ai/agent-chat-ui.git
cd agent-chat-ui
pnpm install
pnpm dev
```

## Agent Configuration

To connect Agent Chat UI to your agent, provide:

- **Graph ID**: Your graph name from the `graphs` section in `langgraph.json`
- **Deployment URL**: Agent server endpoint (e.g., `http://localhost:2024` for local, or your deployed URL)
- **LangSmith API key**: Optional -- "not required if you're using a local Agent server"

The interface automatically retrieves interrupted threads from your configured agent once connected.
