# Agent Chat UI Documentation Summary

## Overview
Agent Chat UI is a Next.js application that "provides a conversational interface for interacting with any LangChain agent." It features real-time chat, tool visualization, time-travel debugging, and state forking capabilities, designed to work with agents created using the `create_agent` function.

## Getting Started

**Hosted Option:**
Users can visit the live version at agentchat.vercel.app, enter their agent's deployment URL or local server address, and begin chatting immediately with automatic tool call rendering.

**Local Development:**
Two setup methods are available:

**Method 1 — `npx create-agent-chat-app`:**
```bash
npx create-agent-chat-app --project-name my-chat-ui
cd my-chat-ui
pnpm install
pnpm dev
```

**Method 2 — Git clone:**
```bash
git clone https://github.com/langchain-ai/agent-chat-ui.git
cd agent-chat-ui
pnpm install
pnpm dev
```

Both approaches require installing dependencies with pnpm and running `pnpm dev` to start development.

## Configuration Steps

To connect Agent Chat UI to an agent, users must provide:
- **Graph ID**: The graph name from the `langgraph.json` file
- **Deployment URL**: The agent server's endpoint (local or deployed)
- **LangSmith API key** (optional): Unnecessary for local Agent servers; only needed for certain deployment scenarios

## Key Features

- Automatic detection and rendering of tool calls and interrupts
- Built-in support for tool result messages
- Time-travel debugging and state forking for advanced debugging workflows
- Customizable message display options
- Open-source codebase available for adaptation
- Support for generative UI implementation

The interface automatically fetches interrupted threads from connected agents once properly configured.

## Headless Tools

Headless tools split tool execution between server and browser. The server-side agent exposes a tool schema, but the actual browser/device API runs on the client through a registered implementation. This is useful when the action needs browser-only capabilities such as clipboard, local file picker, camera, notifications, or DOM APIs.

Typical flow:

1. Register a tool schema on the agent.
2. In the frontend, pass the browser implementation to `useStream`.
3. Render tool progress/results inline with the same tool-call UI used for normal server tools.

Use headless tools when the model should decide **when** a user-device action is needed, but the server should not receive direct access to that device capability.

## Custom Stream Channels

Custom stream channels extend `useStream` beyond built-in message/update/custom callbacks. They are useful for app-specific server-side events such as progress bars, document preview state, retrieval traces, or background task status.

| Hook | Use when |
|------|----------|
| `useExtension` | UI needs the latest payload for a named channel |
| `useChannel` | UI needs raw buffered events for replay, logs, or timelines |

Choose `useExtension` for state-like UI and `useChannel` for event-log UI.
